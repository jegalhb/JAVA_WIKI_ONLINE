# AGENTS Instructions

## Build Artifacts Policy
- Never create or generate `CLASS` binary code documents/files (`*.class`) as part of documentation or source content.
- Do not place `*.class` files under source/document folders.
- If compilation outputs `*.class` files, keep them only in build/output directories and exclude them from docs.
